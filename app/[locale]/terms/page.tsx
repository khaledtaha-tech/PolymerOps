import { PageHeading } from '@/components/site-shell';import { copy,isLocale } from '@/lib/i18n';import { notFound } from 'next/navigation';
export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <><PageHeading eyebrow={copy[locale].terms} title={copy[locale].terms}/><div className="shell max-w-3xl py-14 leading-8 text-slate-700">{locale==='ar'?'المحتوى الفني عام ولا يُعد اعتمادًا لخلطة أو خامة أو إعداد تشغيل. يحدد عرض منفصل نطاق الخدمات المهنية وأتعابها. إرسال الطلب لا ينشئ تعاقدًا ولا يضمن التوظيف. أي قرار تصنيع أو اعتماد يجب أن يستند إلى مراجعة فنية وتجارب واختبارات مناسبة.':'Technical content is general guidance and does not approve a formulation, material, or operating setting. A separate proposal defines the scope and fees of professional services. Submitting a request does not create a contract or guarantee employment. Production and approval decisions require suitable technical review, trials, and testing.'}</div></>}
