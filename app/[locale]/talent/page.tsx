import Link from 'next/link';import { PageHeading } from '@/components/site-shell';import { copy,isLocale } from '@/lib/i18n';import { notFound } from 'next/navigation';
export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const t=copy[locale];return <><PageHeading eyebrow={t.talent} title={t.talent} description={t.noGuarantee}/><div className="shell grid gap-5 py-16 md:grid-cols-2"><div className="card p-9"><p className="eyebrow">01</p><h2 className="mt-5 text-2xl font-bold">{t.talentFactory}</h2><p className="mt-4 leading-7 text-slate-600">{t.talentFactoryBody}</p><Link className="btn-primary mt-8" href={`/${locale}/request?type=factory-talent`}>{t.request}</Link></div><div className="card p-9"><p className="eyebrow">02</p><h2 className="mt-5 text-2xl font-bold">{t.talentCandidate}</h2><p className="mt-4 leading-7 text-slate-600">{t.talentCandidateBody}</p><Link className="btn-outline mt-8" href={`/${locale}/request?type=candidate`}>{t.send}</Link></div></div></>}
