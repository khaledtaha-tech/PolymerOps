import Link from 'next/link';import { PageHeading } from '@/components/site-shell';import { copy,isLocale } from '@/lib/i18n';import { notFound } from 'next/navigation';
export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const t=copy[locale];return <><PageHeading eyebrow={t.consulting} title={t.capabilities} description={t.capabilitiesIntro}/><section className="shell grid gap-4 py-16 md:grid-cols-2">{t.serviceTypes.map((s,i)=><div className="card p-7" key={s}><span className="eyebrow">0{i+1}</span><h2 className="mt-5 text-xl font-bold">{s}</h2><p className="mt-3 leading-7 text-slate-600">{t.serviceDescriptions[i]}</p></div>)}</section><div className="shell pb-16"><Link className="btn-primary" href={`/${locale}/request?type=consulting`}>{t.request}</Link></div></>}
