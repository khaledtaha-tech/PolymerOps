import type { MetadataRoute } from 'next';import { getContent } from '@/lib/db';
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.SITE_URL||'http://localhost:3000';const routes=['','services','consulting','software','materials','talent','insights','request','privacy','terms'];const list:MetadataRoute.Sitemap=[];for(const locale of ['ar','en'])for(const path of routes)list.push({url:`${base}/${locale}${path?`/${path}`:''}`,changeFrequency:path?'monthly':'weekly'});for(const kind of ['service','software','material','insight'] as const){const segment={service:'services',software:'software',material:'materials',insight:'insights'}[kind];for(const item of await getContent(kind))for(const locale of ['ar','en'])list.push({url:`${base}/${locale}/${segment}/${item.slug}`,lastModified:item.updated_at})}return list}
