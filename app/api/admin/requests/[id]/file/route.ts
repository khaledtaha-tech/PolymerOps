import { NextResponse } from 'next/server';import { isAdmin } from '@/lib/auth';import { database } from '@/lib/db';
export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){if(!await isAdmin())return NextResponse.json({error:'Unauthorized'},{status:401});const {id}=await params;const number=Number(id);if(!Number.isSafeInteger(number)||number<1)return NextResponse.json({error:'Invalid id'},{status:400});const result=await database().query<{file_data:Buffer;filename:string}>('SELECT file_data,filename FROM request_files WHERE request_id=$1 LIMIT 1',[number]);if(!result.rows.length)return NextResponse.json({error:'Not found'},{status:404});return new NextResponse(new Uint8Array(result.rows[0].file_data),{headers:{'content-type':'application/pdf','content-disposition':`attachment; filename="${number}.pdf"`,'cache-control':'no-store','x-content-type-options':'nosniff'}})}
