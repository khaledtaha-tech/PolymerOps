'use client';import { useState,type FormEvent } from 'react';import { useRouter } from 'next/navigation';
export function AdminLogin(){const router=useRouter();const [error,setError]=useState('');async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const response=await fetch('/api/admin/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:form.get('email'),password:form.get('password')})});if(response.ok){router.push('/admin');router.refresh()}else setError('Invalid credentials')}return <form onSubmit={submit} className="card mx-auto max-w-md space-y-5 p-8"><h1 className="text-2xl font-bold">Administrator sign in</h1><label className="block">Email<input className="field mt-2" name="email" type="email" required/></label><label className="block">Password<input className="field mt-2" name="password" type="password" required/></label>{error&&<p role="alert" className="text-red-700">{error}</p>}<button className="btn-primary" type="submit">Sign in</button></form>}
