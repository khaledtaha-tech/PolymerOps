import Link from 'next/link';
import { copy, type Locale } from '@/lib/i18n';
const links = [['services','services'],['consulting','consulting'],['software','software'],['materials','materials'],['talent','talent'],['insights','insights']] as const;
export function Header({locale, brand}:{locale:Locale;brand:string}) { const t=copy[locale]; return <header className="sticky top-0 z-40 border-b border-slate-200 bg-white/95 backdrop-blur"><div className="shell flex min-h-20 items-center justify-between gap-4"><Link href={`/${locale}`} className="flex items-center gap-2 text-xl font-black tracking-tight"><span className="grid size-9 place-items-center rounded bg-[#0c526c] text-white">P</span><span>{brand}</span></Link><nav className="hidden items-center gap-5 text-sm font-semibold text-slate-600 lg:flex">{links.map(([key,path])=><Link className="hover:text-[#0c526c]" key={key} href={`/${locale}/${path}`}>{t[key]}</Link>)}</nav><div className="flex items-center gap-3"><Link className="text-sm font-bold text-[#0c526c]" href={`/${locale==='ar'?'en':'ar'}`}>{locale==='ar'?'EN':'عربي'}</Link><Link className="btn-primary hidden text-sm md:inline-flex" href={`/${locale}/request`}>{t.request}</Link></div></div><nav className="shell flex gap-5 overflow-x-auto pb-3 text-sm font-semibold text-slate-600 lg:hidden">{links.map(([key,path])=><Link className="shrink-0" key={key} href={`/${locale}/${path}`}>{t[key]}</Link>)}</nav></header> }
export function Footer({locale,brand}:{locale:Locale;brand:string}) {const t=copy[locale];return <footer className="bg-[#0b2539] py-14 text-slate-200"><div className="shell grid gap-10 md:grid-cols-3"><div><p className="text-xl font-bold text-white">{brand}</p><p className="mt-3 max-w-xs text-sm leading-7 text-slate-300">{t.footer}</p></div><div className="grid grid-cols-2 gap-3 text-sm">{links.map(([key,path])=><Link key={key} href={`/${locale}/${path}`}>{t[key]}</Link>)}</div><div className="flex flex-col items-start gap-3 text-sm"><Link href={`/${locale}/request`}>{t.request}</Link><Link href={`/${locale}/privacy`}>{t.privacy}</Link><Link href={`/${locale}/terms`}>{t.terms}</Link></div></div><div className="shell mt-12 border-t border-white/15 pt-5 text-xs text-slate-400">© {new Date().getFullYear()} {brand}</div></footer>}
export function PageHeading({eyebrow,title,description}:{eyebrow:string;title:string;description?:string}) {return <div className="border-b border-slate-200 bg-white py-16 md:py-24"><div className="shell"><p className="eyebrow mb-5">{eyebrow}</p><h1 className="section-title max-w-3xl">{title}</h1>{description&&<p className="mt-5 max-w-2xl text-lg leading-8 text-slate-600">{description}</p>}</div></div>}
